[pytest]
markers =
    gate: an invariant whose failure means a recorded number could be wrong, or
        the seal could be breached. `pytest -m gate` is what the research cycle
        runs before it evaluates anything, and NOTHING else may stop research.

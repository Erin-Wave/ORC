# ORC runtime.  Deliberately small: the whole engine is numpy plus a parquet
# reader.  No backtesting framework, no ML stack, no GPU.
numpy>=2.0
polars>=1.0
pyarrow>=15.0
scipy>=1.11
requests>=2.31
pytest>=8.0
